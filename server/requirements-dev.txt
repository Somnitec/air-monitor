# Test/dev-only dependencies — NOT needed to run the service.
# Rebuild the dev env with:
#   .venv/bin/python -m pip install -r requirements-dev.txt
-r requirements.txt
pytest>=9.0
pytest-asyncio>=1.0      # for future @pytest.mark.asyncio tests; the current suite uses unittest.IsolatedAsyncioTestCase
httpx2>=2.5              # fastapi/starlette TestClient (test_server.py) needs it — starlette 1.x moved to httpx 2.x
