.pio/
.pioenvs/
.piolibdeps/
.vscode/
.idea/
.DS_Store
*.swp
build/
.claude/settings.local.json
# real credentials/coordinates — ignored in EVERY directory (root, src/, firmware/src/),
# not just the repo root; the old root-only rule let firmware/src/secrets.h get committed
secrets.h

# server/ runtime artifacts
server/.venv/
server/data/
server/.env
__pycache__/
*.db
*.db-wal
*.db-shm